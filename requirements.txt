# Python dependencies for GStreamer Pipeline project
# Install with: pip install -r requirements.txt
#
# Requires system packages to be installed first — see README.md

# GObject Introspection bindings for GStreamer (PyGObject)
PyGObject==3.46.0

# Cairo graphics bindings — required dependency of PyGObject
pycairo==1.29.0

# Frame buffer manipulation for appsink/appsrc
numpy==2.4.4

# OpenCV — used for image processing only (not for display or capture)
# Note: opencv-python from PyPI does NOT include GStreamer support on ARM.
#       Use PyGObject (gi.repository.Gst) for all pipeline control.
opencv-python==4.13.0.92
